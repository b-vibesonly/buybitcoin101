---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import BackToHome from '../components/BackToHome.astro';
import PageNav from '../components/PageNav.astro';
---

<BaseLayout title="Am I Too Late for Bitcoin?">
    <Header />
    
    <main class="main-content">
      <div class="content-container">
        <Breadcrumbs current="Am I Too Late" />
        <h1>NO, YOU ARE NOT TOO LATE</h1>
        <ul>
          <li>How many people do you know that own bitcoin?</li>
          <li>How many people do you know that understand bitcoin?</li>
          <li>Bitcoiners believe that bitcoin is undervalued, misunderstood, and underappreciated.</li>
        </ul>
        <br />
        <p>Anyone buying bitcoin today is still early.</p>
        <BackToHome />
      </div>
      <PageNav />
    </main>

    <style>
      .content-container {
        padding: var(--spacing-lg);
        background: rgba(255, 255, 255, 0.8);
        border-radius: 8px;
        border: 1px solid var(--color-border);
        box-shadow: 0 2px 4px rgba(120, 119, 108, 0.1);
        max-width: 800px;
        margin: 0 auto var(--spacing-lg) auto;
      }
      
      h1 {
        color: var(--color-text);
        margin-bottom: var(--spacing-lg);
        padding-bottom: var(--spacing-sm);
        border-bottom: 2px solid var(--color-border);
      }
      
      p {
        margin-bottom: var(--spacing-md);
        line-height: 1.6;
      }
      
      ul {
        margin-left: var(--spacing-md);
        margin-bottom: var(--spacing-md);
      }
      
      li {
        margin-bottom: var(--spacing-xs);
      }
    </style>
</BaseLayout>